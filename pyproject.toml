[project]
name = "check-kafka-connector-status"
version = "0.01.00.000"
description = "Check the status of Kafka Connect connectors and their tasks"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.31.0",
]
